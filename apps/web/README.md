# Dynamic NFT KRNL Web

Next.js portal UI for Dynamic NFT KRNL.

The app includes brand-owner and NFT-owner portal flows. It reads the backend URL from `NEXT_PUBLIC_API_BASE_URL` and does not import workflow templates directly.

Run from the repository root:

```sh
pnpm dev:web
pnpm build:web
```
